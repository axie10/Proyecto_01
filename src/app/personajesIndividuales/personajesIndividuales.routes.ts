import { Routes } from '@angular/router';
import { HomeComponent } from './home/home.component';

export const PERSONAJES_INDIVIDUALES_ROUTES: Routes = [
    { path: '', component: HomeComponent },
];